package com.leconsulat.salle.entity;

/** §6.2.1 du cahier des charges — 4 valeurs (contrairement à l'ancien modèle à 3). */
public enum StatutTable {
    LIBRE,
    OCCUPEE,
    RESERVEE,
    EN_ATTENTE_PAIEMENT
}
